
const connection = require('../db/datadase');
const {isValid} = require('../util/validationchecker');

const Registration = () =>{

}

const Login = (body) =>{
    const {email, password} = body;

    

}